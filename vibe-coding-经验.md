# Vibe Coding 经验 — 给指挥员的指南

> 我是你的开发 agent。这份文档写给作为指挥员的你，旨在帮你提升 vibe coding 效率和产出质量。

---

## 一、项目启动前：先把骨架搭好

**不要一上来就叫 agent 写代码。** 先花半小时和 agent 一起做规划。

### 1.1 项目文档先行

启动任何新功能之前，确保这些文件存在且是最新的：

| 文件 | 作用 | agent 读写权限 |
|------|------|:---:|
| CLAUDE.md | 项目技术栈、代码结构、关键约定 | 自动读 |
| IDEAS.md | 功能池、优先级、已完成清单 | 手动更新 |
| LESSONS.md | 踩坑记录、禁止行为 | 自动参考 |

### 1.2 用 IDEAS.md 对齐优先级

不急着做所有事情。每次开发前看一眼 IDEAS.md，选优先级最高的一个来做。

---

## 二、开发中：精准反馈加速迭代

### 2.1 测试反馈要具体

agent 不能看到你的屏幕，所以你的反馈决定了修复效率。

| 差的反馈 | 好的反馈 |
|----------|---------|
| "不行，有 bug" | "**放大到 150% 后**，往下拖节点时节点会**往左上方飞**" |
| "还是没修好" | "**缩小到 50% 后**，往上拖节点时节点往**右下方走**，偏差约 20px" |
| "用不了" | "点击节点后节点**粘在鼠标上甩不掉**，怎么回事" |

好反馈的公式：**什么操作 + 什么条件（缩放比例等）+ 什么现象 + 偏差方向和大小**

### 2.2 成功后说"成功了"

这不是客套——是对 agent 的精准信号。agent 能据此：
- 确认方案正确，记录到 LESSONS.md
- 终结这个 bug 的修复循环
- 进入下一个任务

### 2.3 一个时间段只做一个板块

不要一次提 5 个需求。agent 会忽略或搞混。**修完这个 bug、验证通过 → 再做下一个。**

---

## 三、发现 bug 时：先复现再描述

### 3.1 复现步骤越精确越好

```
1. 打开 http://localhost:5173/
2. 创建一个笔记本，添加 2 个节点
3. 把页面缩放到 150%
4. 按住"节点 A"往下拖 50px
5. 观察：节点 A 往左上角偏了约 30px
```

### 3.2 附上缩放比例、浏览器、步骤

很多 bug 只在特定条件下触发（比如 `scale=1.5` 正常但 `scale=2` 有问题）。把这些条件说清楚。

---

## 四、保护你的项目

### 4.1 数据安全

- 已实现自动备份：首页点"开启自动备份"，选一个本地文件夹
- 手动导出也随时可用
- 定期检查备份文件夹里有没有最新的 JSON 文件

### 4.2 代码版本

- 每次做完一个板块 → `git add` + `git commit` + `git push`
- commit message 用中文描述改动内容
- 出问题时可以 `git log` 回溯，或 `git revert` 回退

---

## 五、做产品的思维

### 5.1 功能做完 vs 体验做完

拖拽"做完"是节点能移动。拖拽"体验做完"是：
- 正常缩放能拖
- 放大 200% 能拖
- 缩小 50% 能拖
- 没有偏移，没有粘附
- 在以上条件下都验证过了

### 5.2 一个板块做完再开始下一个

现在的节奏是好的：
1. 把当前 bug 修彻底
2. 验证通过
3. 提交推送
4. 再选下一个任务

---

## 六、提示词模板库

### 模板 A：新功能请求
```
功能：[一句话描述]
用户操作步骤：
  1.
  2.
  3.
预期行为：
约束条件：[性能/兼容/边界]
```

**实例**：
```
功能：选中单个节点后，右侧弹出一个面板显示该节点的所有关联。
用户操作步骤：
  1. 在画布上点击节点 A
  2. 点击底部工具栏的"查看关联"按钮
预期行为：右侧弹出面板，列出 A 与每个关联节点的连线内容和方向。
约束条件：面板不能遮挡画布上的节点。
```

### 模板 B：Bug 报告
```
现象：[描述看到的问题]
复现步骤：
  1. 打开 [URL]
  2. 做 [操作]
  3. 观察 [结果]
条件：缩放比例=[X]%，浏览器=[Chrome/Edge]，操作系统=[Win/Mac]
预期行为：[应该发生什么]
偏差：[方向和大小]
```

**实例**：
```
现象：放大到 150% 后，往下拖拽节点时节点往左上方偏移。
复现步骤：
  1. 打开 http://localhost:5173/
  2. 进入一个笔记本，添加节点
  3. 缩放至 150%
  4. 拖拽任意节点向下移动
  5. 观察节点向左上方偏移
条件：缩放=150%，浏览器=Chrome，操作系统=Windows
预期行为：节点跟随鼠标移动，方向一致
偏差：节点往左上方偏约 30-50px
```

### 模板 C：功能验证请求
```
改了什么：[一句话]
在哪验证：[URL]
验证步骤：
  1. [正常状态下的操作]
  2. [边界状态（缩放/大量节点/空状态）]
  3. [检查特定元素是否出现/消失]
```

**实例**：
```
改了什么：修复连接线不显示的 bug
在哪验证：http://localhost:5173/
验证步骤：
  1. 添加节点 A 和节点 B
  2. 选中两个节点，点"建立联系"
  3. 检查：SVG 连线是否显示在画布上
  4. 缩放至 150%，检查连线位置是否仍正确
```

### 模板 D：代码审查请求
```
范围：[文件路径]
关注点：[性能/安全/兼容性/代码风格]
参考：[相关的现有功能/bug]
```

**实例**：
```
范围：src/components/MindMap.tsx 的 handlePointerMove
关注点：拖拽性能，缩放下的坐标计算是否正确
参考：之前的 bug — 放大后拖拽偏移
```

### 模板 E：项目规划请求
```
目标：[想达到什么]
现状：[当前项目状态]
约束：[时间/技术栈/用户量]
优先级：[必须做 > 应该做 > 可以做]
```

**实例**：
```
目标：让其他人也能用这个知识链接器
现状：本地可用，已部署到 Vercel，已支持 PWA
约束：无需后端，免费，数据本地存储
优先级：确保数据安全 > 让 URL 公开可访问 > 提升 UI 体验
```

---

## 七、进阶建议

### 7.1 让 agent 主动规划而非被动执行

| 被动的指令 | 主动的指令 |
|------------|-----------|
| "帮我加个功能" | "我想实现 X，你觉得有哪些方案？各有什么优劣？" |
| "修复这个" | "分析一下这个 bug 的根因可能是什么？" |
| "改一下" | "这段代码有什么问题？" |

**用"你觉得""分析""推荐"这类词**，agent 会切换到分析和规划模式，而不是直接写代码。

### 7.2 遇到不确定的问题先让 agent 调研

不要自己猜答案。问：
- "Obsidian 有插件能做到这个吗？帮我搜一下"
- "现在市场上类似的产品是什么状态？"
- "这个方案有什么隐患？"

### 7.3 利用 skill 体系

你已安装了大量 skill。遇到相关任务时主动试试：
- `/improve-codebase-architecture` — 出架构审查
- `/prototype` — 快速做原型验证方案
- `/diagnose` — 硬 bug 调试流程

### 7.4 让 agent 解释它做了什么

如果交付的东西不符合预期，不要只说"不行"——问它：
- "你这个方案是怎么想的？"
- "有没有更简单的做法？"
- "为什么不用 [另一种方案]？"

这会让 agent 暴露隐藏的假设，帮你发现理解偏差。

### 7.5 维护 CLAUDE.md 是你的事

CLAUDE.md 是 agent 每次打开项目的入口上下文。过时的内容会误导 agent：
- 功能列表变了 → 更新
- 技术方案变了（比如拖拽从 DOM 直操改成世界坐标步长）→ 更新
- 加了这个文件本身 → 也加进去

---

## 八、总结：你作为指挥员能做的 6 件事

1. **启动前花半小时对齐** — 看 IDEAS.md，用模板 A 或 E 描述需求
2. **给精准的 bug 反馈** — 用模板 B，操作+条件+现象+方向
3. **每次只做一个事** — 一个板块修完再开下一个
4. **修完说"成功了"** — 给 agent 信号记录经验
5. **小步提交** — 每个 commit 一句话，方便回溯
6. **保持 CLAUDE.md 更新** — 它是 agent 的眼睛，过时了就会出错

---

> 写于 2026-06-18，基于知识链接器项目的实战踩坑经验。
> 这份文件是给你（指挥员）看的，不是给 agent 看的。
